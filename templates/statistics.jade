#minimum-right-title

#minimum-right-content(style="padding-top:15px;")
  h3= "Es hat " + totalBoulders + " Boulders an der Wand, allez!"

  p(style="width: 85%;") Willkommen in der Welt der Minimum-Boulders. Hier findest du aktuelle Infos rund um die Boulderprobleme.

  p(style="width: 85%;") Du kannst Boulders bewerten indem du deinem Lieblingsboulder Sternchen (*) verschenkst.

  div.chartlist
    ul.chartlist
      - var colors = ["#ffcc00", "#33cc00", "#ff7900", "#0000ff", "#ff0000", "#ffffff"]
      - var color_labels = ["yellow", "green", "orange", "blue", "red", "white"]
      - for (var id in histogram)
        li
            a(href="/stats/grade/#{color_labels[id]}")&nbsp;
            span.count
              #{histogram[id]}
            span.index(style="width: #{histogram[id]}%; background: #{colors[id]}")
              = histogram[id]%


  div(style="padding-top:20px;")
    h4= "Top 5 Boulders:"
    ul#boulderlist(style="padding-left:70px;")
      - for (var id in top5)
        li
          div(class="boulder", style="margin-bottom:5px;")
            -for (var setter in top5[id].prettySetters)
              a(href="/setter/#{top5[id].prettySetters[setter]}")
                img(class="mini_avatar", style="float:left;", src="/avatars/#{top5[id].prettySetters[setter]}.jpg")

            div(class="description")
              span(class="#{top5[id].color}", style="display:inline-block;width:70px;")
                a(href="/boulder/#{top5[id].id}", class="#{top5[id].color}")
                  = top5[id].gradenr
              -if (top5[id].stars.length)
                span(style="display:inline-block;width: 30px;")
                = top5[id].mean_stars
